import styled from "styled-components";
import svg from "../images/Subtle-Prism.svg";

const Section = styled.main`
  width: 100%;
  display: flex;
  justify-content: flex-end;
  align-items: center;
  background-image: url(${svg});
`;

const Styled = {
  Section,
};

export default Styled;
